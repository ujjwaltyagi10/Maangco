# Backend auth API base URL
# Copy this file to .env and fill in your values
VITE_AUTH_API_BASE_URL=http://localhost:4000/

# Mixpanel project token — get from mixpanel.com → Project Settings → Access Keys
# Optional — event tracking silently no-ops if unset
VITE_MIXPANEL_TOKEN=your-mixpanel-project-token

